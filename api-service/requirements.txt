# ============================
# 课狐 ClassFox Lite - 后端依赖清单（Slim：webspeech + sherpa-onnx）
# ============================

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# 跨域支持
python-multipart==0.0.20

# PPT 解析
python-pptx==1.0.2

# PDF 解析
pypdf>=4.0.0

# Word 文档解析
python-docx>=1.1.0

# 音频录制（sherpa 本地识别采集麦克风用）
pyaudio==0.2.14

# OpenAI Compatible API 调用
openai==1.58.1

# 环境变量
python-dotenv==1.0.1

# WebSocket 支持 (FastAPI 自带，但需要 websockets 库)
websockets==14.1

# 本地流式语音识别 (sherpa-onnx streaming Zipformer, CPU)
sherpa-onnx>=1.13.0

# 异步文件操作
aiofiles==24.1.0
